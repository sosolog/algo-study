import sys
def solution(participant, completion):
    dict = {}
    sum = 0
    for part in participant:
        dict[hash(part)] = part
        sum += hash(part)
    for comp in completion:
        sum -= hash(comp)
    dict[sum]
    return dict[sum]